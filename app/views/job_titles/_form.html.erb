<div class="contenedor_frm_mant" id="contenedor_frm_mant_job_titles">
<%= form_for(@job_title) do |f| %>
  <% if @job_title.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@job_title.errors.count, "error") %> prohibited this job_title from being saved:</h2>

      <ul>
      <% @job_title.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="grupo_fields">
    <div class="field">
      <span class="label_frm"><%= f.label :name %></span> <span class="text_field_input"><%= f.text_field :name %></span>
    </div>
    <div class="actions">
      <%= f.submit %> <%= link_to 'Cancelar', job_titles_path %>
    </div>
  </div>
<% end %>
</div>
